{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "environment": "*",
  "authors": [
    "${mod_authors}"
  ],
  "contributors": [
  ],
  "contact": {},
  "license": "${mod_license}",
  "icon": "${mod_id}_logo_128x128.png",
  "entrypoints": {
    "main": [
      "de.keksuccino.drippyloadingscreen.DrippyLoadingScreenFabric"
    ],
    "client": [],
    "server": []
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_short_version}",
    "konkrete": ">=${konkrete_version}",
    "fancymenu": ">=${fancymenu_version}",
    "minecraft": ">=${minecraft_version} <${minecraft_version_upper_bound}",
    "java": ">=${java_version}"
  }
}
